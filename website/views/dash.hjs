<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel='stylesheet' href='/stylesheets/c3.min.css'/>
    <script src="/javascripts/moment.js"></script>
    <!--<script src="/javascripts/Chart.min.js"></script>-->
    <script src="/javascripts/d3.min.js"></script>
    <!--<script src="/javascripts/c3.js"></script>-->
    <script src="/javascripts/jquery-1.11.3.min.js"></script>
    <script type="text/javascript" src="/javascripts/dygraph-combined-dev.js"></script>
</head>
<body>

    <h3>Heads up readouts</h3>
    <div class="headsUp">
        <div class="dataBrick">
            Hot Tub Temp:
            <span class="bigTemp">{{ latest.tub.temp }}</span>
            <span class="lastHeardDate">(last heard from: {{ latest.tub.published_at }})</span>
        </div>
        <div class="dataBrick">
            Coop Temp:
            <span class="bigTemp">{{ latest.coop.temp }}</span>
            <span class="lastHeardDate">(last heard from: {{ latest.coop.published_at }})</span>
        </div>
        <div class="dataBrick">
            Weather Info:
            <span class="bigTemp">{{ latest.weather.temp1 }}</span>
            <span class="bigTemp">{{ latest.weather.soilTemp }}</span>
            <span class="bigTemp">{{ latest.weather.humidity }}</span>
            <span class="bigTemp">{{ latest.weather.rain }}</span>
            <span class="bigTemp">{{ latest.weather.wind_mph }}</span>

            <span class="lastHeardDate">(last heard from: {{ latest.weather.published_at }})</span>
        </div>
    </div>


    <div class="chartContainer">
        <div id="tubChart" class="innerChart" >hot tub temperature history loading...</div>
    </div>

    <div class="chartContainer">
        <div id="coopChart" class="innerChart">coop temperature history loading...</div>
    </div>

    <h3>Weather History</h3>
    <div class="chartContainer">
        <div id="weatherChart" class="innerChart">weather history loading...</div>
    </div>

    <h3>Air Pressure / Altitude History</h3>
    <div class="chartContainer">
        <div id="pressureChart" class="innerChart">weather history loading...</div>
    </div>

    <script src="/pages/dash.js"></script>
</body>
</html>
